TEST CASES (manual)

1) Add + persist
- Run CLI
- Add car id=1 type=sport brand=A model=M1 year=2010 scale=1:64 condition=new price=10
- Exit (0)
- Run CLI again -> List all -> car id=1 exists

2) Modify
- Modify id=1 -> change brand to "Hot Wheels"
- List all -> brand updated
- Exit -> restart -> brand saved

3) Remove
- Remove id=1
- List all -> empty
- Restart -> still empty

4) Import CSV
- Prepare CSV with header:
  id,brand,model,year,scale,condition,price,notes,type
- Import CSV via CLI/GUI
- List/group -> shows imported cars

5) Export CSV
- Export to out.csv
- Open file -> correct columns and rows

6) Group by type
- Add 2-3 cars разных типов
- Group by type -> each group prints correct items

7) Collection operators (developer check)
- In python console:
  from hash_collection import HashCollection
  c=HashCollection(); c<< (1,"a"); assert c[1]=="a"
  c2=c.copy(); c2.add(2,"b"); inter=c & c2; assert inter==c
